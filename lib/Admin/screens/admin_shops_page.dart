import 'package:flutter/material.dart';

class AdminShopsPage extends StatelessWidget {
  const AdminShopsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Manage Shops"));
  }
}
