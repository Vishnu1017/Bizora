import 'package:flutter/material.dart';

class OwnerAnalytics extends StatelessWidget {
  const OwnerAnalytics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Sales Analytics",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
